/**
 * [MVVM : AutoDetect Handlers]
 * Gestionnaires d'événements DOM (si nécessaire).
 */

const AutoDetectHandlers = {
    init() {
        // Pour l'instant, les événements sont gérés par les attributs HTML (onclick, etc.)
        // On pourrait ici attacher des listeners dynamiques si besoin.
    }
};
