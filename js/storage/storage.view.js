/**
 * Vue pour le module Storage
 * Gère les retours visuels (alertes, logs).
 */
class StorageView {
    constructor() {
        // Pas de DOM à gérer spécifiquement pour le moment
    }

    showError(message) {
        alert(message);
    }
}
