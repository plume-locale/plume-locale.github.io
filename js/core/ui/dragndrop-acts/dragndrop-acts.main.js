/**
 * Main entry point for Drag and Drop Acts
 */
document.addEventListener('DOMContentLoaded', () => {
    // Initialization is usually handled by other modules calling the setup functions
    // (like structure.view.js) but we can put global init here if needed.
    // dragNDropActsView.init();
});
